package com.midnight.core.domain

data class UserScoreModelCore(val id: Int?,
                              val userName: String,
                              val isWin:Boolean,
                              val score:Int)
